# P10: d1_persist_base rerun with patience=150.
parent: ../D_series/d1_persist_base.yaml

lineage:
  parent: d1_persist_base
  changes: {patience: "0 -> 150"}
  description: "Val-gated early stopping rerun"

train:
  patience: 150

output:
  dir: "mpnn_2copies_datamax1_v3/experiments/cleansplit/P_series"
