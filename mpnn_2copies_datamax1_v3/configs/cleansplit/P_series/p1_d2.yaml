# P1: d2_persist_feedback rerun with patience=150.
parent: ../D_series/d2_persist_feedback.yaml

lineage:
  parent: d2_persist_feedback
  changes: {patience: "0 -> 150"}
  description: "Val-gated early stopping rerun"

train:
  patience: 150

output:
  dir: "mpnn_2copies_datamax1_v3/experiments/cleansplit/P_series"
