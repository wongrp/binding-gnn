# P6: d15_virtual_node rerun with patience=150.
parent: ../D_series/d15_virtual_node.yaml

lineage:
  parent: d15_virtual_node
  changes: {patience: "0 -> 150"}
  description: "Val-gated early stopping rerun"

train:
  patience: 150

output:
  dir: "mpnn_2copies_datamax1_v3/experiments/cleansplit/P_series"
